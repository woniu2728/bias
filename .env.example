# Bias Docker infra variables
#
# 从当前版本开始，论坛运行时配置默认写入 instance/site.json，
# 不再推荐把站点域名、CORS、CSRF、SECRET_KEY、数据库模式等应用配置长期放在 .env。
#
# 这个文件主要给 Docker Compose 的基础设施层使用：
# - PostgreSQL 容器初始化
# - 端口映射
#
# 推荐默认值：
DB_NAME=bias
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# 如需把 PostgreSQL 暴露到宿主机上的其他端口，可修改 DB_PORT。
# 论坛站点域名请在安装时通过 install_forum 命令传入，例如：
# docker compose exec web python manage.py install_forum --site-domains bias.chat,www.bias.chat ...
